table:
  name: messages
  schema: public
insert_permissions:
- permission:
    backend_only: false
    check: {}
    columns:
    - message
    - sentAt
    set:
      address: x-hasura-user-id
  role: user
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - message
    - address
    - sentAt
    filter: {}
  role: user
