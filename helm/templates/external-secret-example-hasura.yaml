{{- if .Values.localDev }}
{{- else }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: example-hasura
spec:
  secretStoreRef:
    name: fake
    kind: ClusterSecretStore
  target:
    name: example-hasura
  dataFrom:
  - extract:
      key: /example-hasura
{{- end }}
